// Firebase has been replaced with local SQLite + JWT auth.
// This stub keeps type-compatibility with any remaining imports.
export const auth = null
export const db = null
export const storage = null
export const googleProvider = null
